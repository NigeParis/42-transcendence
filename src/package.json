{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "./_shared",
    "./icons"
  ],
  "scripts": {
    "build": "npm run build:ts --workspaces",
    "fclean": "rimraf \"**/dist\"",
    "clean": "rimraf \"**/node_modules\"",
    "install-all": "npm install"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "rimraf": "^5.0.1"
  }
}

