{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "./@shared",
    "./icons"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "fclean": "rimraf \"**/dist\"",
    "clean": "rimraf \"**/node_modules\"",
    "install-all": "npm install"
  },
  "devDependencies": {
    "rimraf": "^5.0.1"
  }
}

