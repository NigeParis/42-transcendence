{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "./@shared",
    "./icons",
    "./auth"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "fclean": "rimraf \"**/dist\"",
    "clean": "rimraf \"**/node_modules\"",
    "install-all": "npm install"
  },
  "devDependencies": {
    "rimraf": "^5.0.1"
  },
  "dependencies": {
    "bindings": "^1.5.0"
  }
}
